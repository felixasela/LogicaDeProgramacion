# Punto 1.2
# Paso 1 El “for” indica que (i) son todos los números de 0 a 21 exclusivo.
# Paso 2 Imprime (i) y con un espacio de separacion (i) Elevado al cubo.
for i in range (21): 
    print(i, i ** 3)
 
# Punto 1.3
# Paso 1 El “for” indica que (i) son los números desde el 9 hasta el 0 exclusivo.
# Paso 2 Imprime (i) que serán los números del 9 al 1.
# Paso 3 Imprime la frase “Feliz año nuevo” al finalizar la cuenta de 9 a 1.
for i in range (10, 0, -1):                              
    print (i)                                                     
print('Feliz año nuevo!')                             

# Punto 1.4
# Paso 1 El “for” indica que (i) toma los valores de la lista. 
# Paso 2 Imprime la palabra “Hola”. 
for i in [0,1,2,3,4]:                                                                                                 
  Print (“Hola”)                 

# Punto 1.5
# Paso 1 El “for” indica que (i) toma todos los valores de 0 hasta 5 exclusivo.
# Paso 2 Imprime el valor de (i) y su valor cuadrado.
for i in range(5):      
  print ("Ahora i vale",i,"y su cuadrado", i**2)    

# Punto 1.6
# Paso 1 El “for” indica que (i) toma los valores de la lista.
# Paso 2 Imprime “Hola. Ahora i vale” mas el valor de i según la lista.
for i in ['JLo', 'Benito', 'Rosalia', 2021]:  
  print ("Hola. Ahora i vale",i)  
  
# Punto 1.7
for n in range(0,8):
print(n)

# Punto 1.8
for n in range(1,13):
print(n)

# Punto 1.9
# Paso 1 Se asigna “C” como variable.
# Paso 2 El “for” determina que el rango sera de 0 a 1000 exclusivo.
# Paso 3 El resultado de (i ** 3) % 10 será asignado a la variable ultimo_digito.
# Paso 4 Si la variable ultimo_digito es igual a 7 se realiza la suma de C + 1 asignando este resultado como “C”.
# Paso 5 Se imprime la variable “C”
c = 0                                               
for i in range(1000):                         
  ultimo_digito = (i ** 3) % 10                       
  if ultimo_digito == 7:                     
    c = c + 1                                     
    print(c)                 

# Punto 2.1
# Ejemplo 1:
for i in [0, 1, 2]:
  for j in [0, 1]:
    print("i vale {i} y j vale {j}")

# Ejemplo 2:
for i in [1, 2, 3]:
  for j in range(i):
    print("i vale {i} y j vale {j}")
